diff --git a/metro.config.js b/metro.config.js
index dba8528..8b09d8b 100644
--- a/metro.config.js
+++ b/metro.config.js
@@ -17,11 +17,19 @@ config.resolver = {
   sourceExts: [...resolver.sourceExts, 'svg'],
   // Ensure module paths include root node_modules
   nodeModulesPaths: [path.resolve(__dirname, 'node_modules')],
+  // Follow npm link symlinks (needed for local dev with linked submodules)
+  unstable_enableSymlinks: true,
   alias: {
     '@': path.resolve(__dirname, 'src'),
   },
 };
 
+// Watch the linked package source directories so Metro picks up changes
+config.watchFolders = [
+  path.resolve(__dirname, '../../packages/wdk-react-native-provider'),
+  path.resolve(__dirname, '../../packages/pear-wrk-wdk'),
+];
+
 // Apply WDK polyfills configuration first (handles Node.js core module polyfills)
 const wdkConfig = configureMetroForWDK(config);
 
diff --git a/package.json b/package.json
index bafed0e..1286977 100644
--- a/package.json
+++ b/package.json
@@ -45,7 +45,7 @@
     "expo-camera": "~17.0.8",
     "expo-clipboard": "~8.0.7",
     "expo-constants": "~18.0.9",
-    "expo-crypto": "^15.0.7",
+    "expo-crypto": "~15.0.8",
     "expo-dev-client": "^6.0.12",
     "expo-font": "~14.0.8",
     "expo-haptics": "~15.0.7",
@@ -87,6 +87,7 @@
     "react-native-worklets": "0.5.1",
     "react-redux": "^9.2.0",
     "sodium-javascript": "^0.8.0",
+    "sodium-native": "5.0.9",
     "sonner-native": "^0.21.1",
     "stream-browserify": "^3.0.0",
     "stream-http": "^3.2.0"
diff --git a/src/app/_layout.tsx b/src/app/_layout.tsx
index 4df44bd..c80d4cf 100644
--- a/src/app/_layout.tsx
+++ b/src/app/_layout.tsx
@@ -23,6 +23,15 @@ const CustomDarkTheme = {
   },
 };
 
+const walletConfig = {
+  indexer: {
+    apiKey: process.env.EXPO_PUBLIC_WDK_INDEXER_API_KEY!,
+    url: process.env.EXPO_PUBLIC_WDK_INDEXER_BASE_URL!,
+  },
+  chains: getChainsConfig(),
+  enableCaching: true,
+};
+
 export default function RootLayout() {
   useEffect(() => {
     const initApp = async () => {
@@ -46,16 +55,7 @@ export default function RootLayout() {
           primaryColor: colors.primary,
         }}
       >
-        <WalletProvider
-          config={{
-            indexer: {
-              apiKey: process.env.EXPO_PUBLIC_WDK_INDEXER_API_KEY!,
-              url: process.env.EXPO_PUBLIC_WDK_INDEXER_BASE_URL!,
-            },
-            chains: getChainsConfig(),
-            enableCaching: true,
-          }}
-        >
+        <WalletProvider config={walletConfig}>
           <NavigationThemeProvider value={CustomDarkTheme}>
             <View style={{ flex: 1, backgroundColor: colors.background }}>
               <Stack
