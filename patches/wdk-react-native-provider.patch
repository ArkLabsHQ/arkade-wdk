diff --git a/package.json b/package.json
index fd6fc94..ea59d93 100644
--- a/package.json
+++ b/package.json
@@ -40,8 +40,8 @@
     "lint": "eslint \"**/*.{js,ts,tsx}\"",
     "clean": "del-cli lib",
     "prepare": "bob build && npm run gen:secret-manager-bundle && npm run gen:worker-bundle",
-    "gen:worker-bundle": "npx bare-pack --target ios-arm64 --target ios-arm64-simulator --target ios-x64-simulator --target android-arm --target android-arm64 --target android-ia32 --target android-x64 --linked --imports node_modules/@tetherto/pear-wrk-wdk/pack.imports.json --out lib/module/services/wdk-service/wdk-worklet.mobile.bundle.js node_modules/@tetherto/pear-wrk-wdk/src/wdk-worklet.js",
-    "gen:secret-manager-bundle": "npx bare-pack --target ios-arm64 --target ios-arm64-simulator --target ios-x64-simulator --target android-arm --target android-arm64 --target android-ia32 --target android-x64 --linked --out lib/module/services/wdk-service/wdk-secret-manager-worklet.bundle.js src/worklet/wdk-secret-manager-worklet.js"
+    "gen:worker-bundle": "npx bare-pack@1 --target ios-arm64 --target ios-arm64-simulator --target ios-x64-simulator --target android-arm --target android-arm64 --target android-ia32 --target android-x64 --linked --imports node_modules/@tetherto/pear-wrk-wdk/pack.imports.json --out lib/module/services/wdk-service/wdk-worklet.mobile.bundle.js node_modules/@tetherto/pear-wrk-wdk/src/wdk-worklet.js",
+    "gen:secret-manager-bundle": "npx bare-pack@1 --target ios-arm64 --target ios-arm64-simulator --target ios-x64-simulator --target android-arm --target android-arm64 --target android-ia32 --target android-x64 --linked --out lib/module/services/wdk-service/wdk-secret-manager-worklet.bundle.js src/worklet/wdk-secret-manager-worklet.js"
   },
   "keywords": [
     "react-native",
diff --git a/src/contexts/types.ts b/src/contexts/types.ts
index aa13609..dc7a3f5 100644
--- a/src/contexts/types.ts
+++ b/src/contexts/types.ts
@@ -37,6 +37,31 @@ export interface WalletContextType extends WalletContextState {
   refreshWalletBalance: () => Promise<void>;
   refreshTransactions: () => Promise<void>;
   unlockWallet: () => Promise<boolean | undefined>;
+
+  // ==========================================
+  // Arkade-specific surface (Phase 2.9)
+  //
+  // These delegate to WDKService and through to the worklet's arkade* RPC
+  // routes. Complex shapes are returned as `unknown` — UI consumers narrow
+  // them with their own types since the upstream PendingSwap shape is large
+  // and the provider intentionally doesn't import @arkade-os/boltz-swap on
+  // the RN side.
+  // ==========================================
+  getBoardingAddress: (index?: number) => Promise<string>;
+  createLightningInvoice: (
+    amount: number,
+    description?: string,
+    index?: number
+  ) => Promise<{ invoice: string; paymentHash: string }>;
+  getPendingLightningReceives: (index?: number) => Promise<unknown[]>;
+  getPendingLightningSends: (index?: number) => Promise<unknown[]>;
+  getSwapHistory: (index?: number) => Promise<unknown[]>;
+  getLightningLimits: (index?: number) => Promise<unknown>;
+  getLightningFees: (index?: number) => Promise<unknown>;
+  waitForLightningPayment: (
+    invoice: string,
+    index?: number
+  ) => Promise<{ txid: string }>;
 }
 
 // Provider configuration
diff --git a/src/contexts/wallet-context.tsx b/src/contexts/wallet-context.tsx
index 4c3dff5..2a7d990 100644
--- a/src/contexts/wallet-context.tsx
+++ b/src/contexts/wallet-context.tsx
@@ -384,6 +384,9 @@ export function WalletProvider({
     dispatch({ type: 'SET_TRANSACTIONS', payload: transactionMap });
   };
 
+  // Arkade methods are pass-throughs to the WDKService singleton; the context
+  // value re-exposes them so consumers can use the useWallet() hook without
+  // needing to know about the underlying service.
   const value: WalletContextType = {
     ...state,
     createWallet,
@@ -392,6 +395,18 @@ export function WalletProvider({
     refreshWalletBalance,
     refreshTransactions,
     unlockWallet,
+    getBoardingAddress: (index) => WDKService.getBoardingAddress(index),
+    createLightningInvoice: (amount, description, index) =>
+      WDKService.createLightningInvoice(amount, description, index),
+    getPendingLightningReceives: (index) =>
+      WDKService.getPendingLightningReceives(index),
+    getPendingLightningSends: (index) =>
+      WDKService.getPendingLightningSends(index),
+    getSwapHistory: (index) => WDKService.getSwapHistory(index),
+    getLightningLimits: (index) => WDKService.getLightningLimits(index),
+    getLightningFees: (index) => WDKService.getLightningFees(index),
+    waitForLightningPayment: (invoice, index) =>
+      WDKService.waitForLightningPayment(invoice, index),
   };
 
   return (
diff --git a/src/services/wdk-service/index.ts b/src/services/wdk-service/index.ts
index 5ae8528..3e5e2d5 100644
--- a/src/services/wdk-service/index.ts
+++ b/src/services/wdk-service/index.ts
@@ -13,6 +13,7 @@ import {
   AssetBalanceMap,
   AssetTicker,
   NetworkType,
+  isArkadeNetwork,
 } from './types';
 import { wdkEncryptionSalt } from './wdk-encryption-salt';
 import {
@@ -22,6 +23,107 @@ import {
   WdkSecretManagerStorage,
 } from './wdk-secret-manager-storage';
 
+// ============================================================================
+// Inline bech32m decoder for Ark addresses (Phase 2b)
+//
+// The provider's RN-side code can't import @arkade-os/sdk's ArkAddress.decode
+// because pulling in the SDK on the React Native JS thread brings sodium-
+// universal, bare-node-runtime, and friends — none of which are RN-compatible.
+// The README's "Temporary Workarounds" section calls for an inline bech32m
+// decoder so the balance workaround stays self-contained until the SDK's
+// Esplora URL becomes configurable. ~80 lines, no deps.
+//
+// Reference: BIP-350. Ark addresses encode 65 bytes:
+//   1 byte version || 32 bytes serverPubKey || 32 bytes vtxoTaprootKey
+// The pkScript queried by the Ark indexer is `OP_1 OP_PUSHBYTES_32 <vtxo>`
+// = `5120 || hex(vtxoTaprootKey)`.
+// ============================================================================
+/* eslint-disable no-bitwise -- bech32m is bit-packed by definition */
+const BECH32M_CHARSET = 'qpzry9x8gf2tvdw0s3jn54khce6mua7l';
+const BECH32M_CONST = 0x2bc830a3;
+
+function bech32mPolymod(values: number[]): number {
+  const GEN = [0x3b6a57b2, 0x26508e6d, 0x1ea119fa, 0x3d4233dd, 0x2a1462b3];
+  let chk = 1;
+  for (const v of values) {
+    const top = chk >>> 25;
+    chk = ((chk & 0x1ffffff) << 5) ^ v;
+    for (let i = 0; i < 5; i++) {
+      if ((top >>> i) & 1) chk ^= GEN[i]!;
+    }
+  }
+  return chk;
+}
+
+function bech32mHrpExpand(hrp: string): number[] {
+  const out: number[] = [];
+  for (let i = 0; i < hrp.length; i++) out.push(hrp.charCodeAt(i) >>> 5);
+  out.push(0);
+  for (let i = 0; i < hrp.length; i++) out.push(hrp.charCodeAt(i) & 31);
+  return out;
+}
+
+function bech32mDecode(addr: string): { hrp: string; words: number[] } {
+  const lower = addr.toLowerCase();
+  if (lower.length > 1023) throw new Error('Ark address too long');
+  const sep = lower.lastIndexOf('1');
+  if (sep < 1 || sep + 7 > lower.length) {
+    throw new Error('Ark address: invalid bech32m structure');
+  }
+  const hrp = lower.substring(0, sep);
+  const data: number[] = [];
+  for (let i = sep + 1; i < lower.length; i++) {
+    const idx = BECH32M_CHARSET.indexOf(lower.charAt(i));
+    if (idx === -1) throw new Error('Ark address: invalid bech32m character');
+    data.push(idx);
+  }
+  if (bech32mPolymod([...bech32mHrpExpand(hrp), ...data]) !== BECH32M_CONST) {
+    throw new Error('Ark address: bech32m checksum mismatch');
+  }
+  return { hrp, words: data.slice(0, data.length - 6) };
+}
+
+function bech32mFromWords(words: number[]): number[] {
+  let acc = 0;
+  let bits = 0;
+  const out: number[] = [];
+  for (const v of words) {
+    if (v < 0 || v > 31) throw new Error('Ark address: invalid 5-bit value');
+    acc = (acc << 5) | v;
+    bits += 5;
+    while (bits >= 8) {
+      bits -= 8;
+      out.push((acc >>> bits) & 0xff);
+    }
+  }
+  if (bits >= 5 || (acc << (8 - bits)) & 0xff) {
+    throw new Error('Ark address: invalid bech32m padding');
+  }
+  return out;
+}
+
+/**
+ * Decode an Ark address (bech32m) into the BTC pkScript that the Ark indexer
+ * expects in its `scripts=` query parameter.
+ *
+ * @internal — exported only for tests.
+ */
+export function arkAddressToPkScript(address: string): string {
+  const { words } = bech32mDecode(address);
+  const data = bech32mFromWords(words);
+  if (data.length !== 65) {
+    throw new Error(
+      `Ark address: expected 65-byte payload (1+32+32), got ${data.length}`
+    );
+  }
+  const vtxoTaprootKey = data.slice(33, 65);
+  const hex = vtxoTaprootKey
+    .map((b) => b.toString(16).padStart(2, '0'))
+    .join('');
+  return `5120${hex}`; // OP_1 (0x51) + OP_PUSHBYTES_32 (0x20) + 32-byte witness program
+}
+/* eslint-enable no-bitwise */
+
 export const SMART_CONTRACT_BALANCE_ADDRESSES = {
   [AssetTicker.USDT]: {
     ethereum: '0xdAC17F958D2ee523a2206206994597C13D831ec7',
@@ -41,6 +143,12 @@ const toNetwork = (n: NetworkType): string => {
   switch (n) {
     case NetworkType.SEGWIT:
       return 'bitcoin';
+    case NetworkType.ARKADE:
+    case NetworkType.ARKADE_BOARDING:
+    case NetworkType.LIGHTNING:
+      // All three variants share one underlying Arkade account on the
+      // worklet side; the variant only chooses which method to call.
+      return 'arkade';
     case NetworkType.ETHEREUM:
       return 'ethereum';
     case NetworkType.TON:
@@ -79,6 +187,9 @@ class WDKService {
   private wdkManager: WdkManager | null = null;
   private secretManager: any | null = null;
   private config: WDKServiceConfig | undefined;
+  // Arkade wallet running on the RN side (not in the Bare worklet).
+  private arkadeAccount: any = null;
+  private arkadeManager: any = null;
 
   private constructor() {}
 
@@ -193,8 +304,13 @@ class WDKService {
       // 3. Clear all keychain storage (entropy, seed, salt)
       await WdkSecretManagerStorage.resetAllData();
 
-      // 4. Reset internal state (but keep worklets alive)
+      // 4. Reset internal state
       this.walletManagerCache.clear();
+      if (this.arkadeManager) {
+        try { await this.arkadeManager.dispose(); } catch (_e) { /* best-effort */ }
+        this.arkadeManager = null;
+        this.arkadeAccount = null;
+      }
 
       // Note: We keep this.wdkManager and this.secretManager references intact
       // as the worklets are still running and can be reused
@@ -205,6 +321,49 @@ class WDKService {
     }
   }
 
+  async initArkadeWallet(seedPhrase: string): Promise<void> {
+    const arkadeConfig = this.config?.chains?.arkade;
+    if (!arkadeConfig?.arkServerUrl) {
+      console.warn('[arkade] Skipping wallet init — arkServerUrl not set');
+      return;
+    }
+    try {
+      // @ts-expect-error — resolved via symlink at runtime
+      const wdk = await import('@arkade-os/wdk');
+      const WalletManagerArkade = wdk.default;
+      const { ExpoArkProvider, ExpoIndexerProvider } = await import(
+        // @ts-expect-error — subpath resolved at runtime by Metro
+        '@arkade-os/sdk/adapters/expo'
+      );
+      const { SQLiteWalletRepository, SQLiteContractRepository } =
+        // @ts-expect-error — subpath resolved at runtime by Metro
+        await import('@arkade-os/sdk/repositories/sqlite');
+      // @ts-expect-error — resolved at runtime
+      const SQLite = await import('expo-sqlite');
+      const db = SQLite.openDatabaseSync('arkade-wallet.db');
+      const executor = {
+        run: (sql: string, params?: any[]) => db.runAsync(sql, params ?? []),
+        get: (sql: string, params?: any[]) => db.getFirstAsync(sql, params ?? []),
+        all: (sql: string, params?: any[]) => db.getAllAsync(sql, params ?? []),
+      };
+      const indexerUrl = arkadeConfig.indexerUrl || arkadeConfig.arkServerUrl;
+      this.arkadeManager = new WalletManagerArkade(seedPhrase, {
+        ...arkadeConfig,
+        arkProvider: new ExpoArkProvider(arkadeConfig.arkServerUrl),
+        indexerProvider: new ExpoIndexerProvider(indexerUrl),
+        storage: {
+          walletRepository: new SQLiteWalletRepository(executor),
+          contractRepository: new SQLiteContractRepository(executor),
+        },
+      });
+      this.arkadeAccount = await this.arkadeManager.getAccount(0);
+    } catch (error) {
+      console.error('[arkade] Failed to initialize wallet on RN side:', error);
+      this.arkadeManager = null;
+      this.arkadeAccount = null;
+    }
+  }
+
   // WDK API Methods
   async createSeed(params: {
     prf: Buffer | ArrayBuffer | string;
@@ -359,6 +518,14 @@ class WDKService {
         network: toNetwork(network),
         accountIndex: index,
       });
+    } else if (network === NetworkType.ARKADE) {
+      if (!this.arkadeAccount) return { address: '' };
+      return { address: await this.arkadeAccount.getAddress() };
+    } else if (network === NetworkType.ARKADE_BOARDING) {
+      if (!this.arkadeAccount) return { address: '' };
+      return { address: await this.arkadeAccount.getBoardingAddress() };
+    } else if (network === NetworkType.LIGHTNING) {
+      return { address: '' };
     } else {
       return await this.wdkManager.getAbstractedAddress({
         network: toNetwork(network),
@@ -375,6 +542,7 @@ class WDKService {
       throw new Error('WDK Manager not initialized');
     }
 
+    const ADDRESS_RESOLUTION_TIMEOUT_MS = 10000;
     const addressPromises = [];
     const networkAddresses: Partial<Record<NetworkType, string>> = {};
     const addressesArr = [];
@@ -382,9 +550,28 @@ class WDKService {
     for (const asset of enabledAssets) {
       for (const networkType of Object.keys(AssetAddressMap[asset])) {
         addressesArr.push({ [networkType]: null });
-        addressPromises.push(
-          this.getAssetAddress(networkType as NetworkType, index)
-        );
+        // Race each per-network address fetch against a hard timeout.
+        // Without this, a single unreachable RPC / bundler (e.g. the
+        // Ethereum provider behind the ERC-4337 abstracted-address flow)
+        // makes the enclosing Promise.allSettled hang indefinitely and
+        // blocks wallet creation. On timeout the entry resolves to null,
+        // matching the existing failure handling below.
+        let timeoutId: ReturnType<typeof setTimeout>;
+        const raced = Promise.race([
+          this.getAssetAddress(networkType as NetworkType, index),
+          new Promise((_, reject) => {
+            timeoutId = setTimeout(
+              () =>
+                reject(
+                  new Error(
+                    `${asset}/${networkType} address resolution timed out after ${ADDRESS_RESOLUTION_TIMEOUT_MS}ms`
+                  )
+                ),
+              ADDRESS_RESOLUTION_TIMEOUT_MS
+            );
+          }),
+        ]).finally(() => clearTimeout(timeoutId));
+        addressPromises.push(raced);
       }
     }
 
@@ -395,18 +582,22 @@ class WDKService {
         // @ts-expect-error
         networkAddresses[key] = (addresses[i] as any).value.address;
       } else {
-        // @ts-expect-error
-        networkAddresses[key] = null;
+        // Leave the entry unset on failure so the map stays a true
+        // Partial<Record<NetworkType, string>>. Consumers already guard
+        // with `if (address)` truthiness checks, and writing `null` here
+        // crashed call sites that did `Object.values(...).map(a => a.toLowerCase())`.
         console.error(
           `Error while resolving wallet address ${key} - err - ${(addresses[i] as any).reason}`
         );
       }
     });
 
-    networkAddresses[NetworkType.POLYGON] =
-      networkAddresses[NetworkType.ETHEREUM];
-    networkAddresses[NetworkType.ARBITRUM] =
-      networkAddresses[NetworkType.ETHEREUM];
+    if (networkAddresses[NetworkType.ETHEREUM]) {
+      networkAddresses[NetworkType.POLYGON] =
+        networkAddresses[NetworkType.ETHEREUM];
+      networkAddresses[NetworkType.ARBITRUM] =
+        networkAddresses[NetworkType.ETHEREUM];
+    }
 
     return networkAddresses;
   }
@@ -432,6 +623,16 @@ class WDKService {
           },
         });
 
+        return Number(quote.fee) / this.getDenominationValue(AssetTicker.BTC);
+      } else if (isArkadeNetwork(network)) {
+        if (!this.arkadeAccount) throw new Error('Arkade wallet not initialized');
+        const value = new Decimal(amount)
+          .mul(this.getDenominationValue(AssetTicker.BTC))
+          .toNumber();
+        const quote = await this.arkadeAccount.quoteSendTransaction({
+          to: recipientAddress,
+          value: value.toString(),
+        });
         return Number(quote.fee) / this.getDenominationValue(AssetTicker.BTC);
       } else if (
         [
@@ -520,6 +721,16 @@ class WDKService {
       });
 
       return response;
+    } else if (isArkadeNetwork(network)) {
+      if (!this.arkadeAccount) throw new Error('Arkade wallet not initialized');
+      const sendParams = {
+        to: recipientAddress,
+        value: new Decimal(amount)
+          .mul(this.getDenominationValue(AssetTicker.BTC))
+          .round()
+          .toString(),
+      };
+      return await this.arkadeAccount.sendTransaction(sendParams);
     } else if (
       [
         NetworkType.ETHEREUM,
@@ -571,6 +782,83 @@ class WDKService {
     }
   }
 
+  // ==========================================================================
+  // Arkade-specific methods (Phase 2.7)
+  //
+  // Each method calls the matching arkade* HRPC route on the worklet and parses
+  // any JSON-encoded payloads. The complex shapes (PendingReverseSwap,
+  // PendingSubmarineSwap, FeesResponse, LimitsResponse, ArkTransaction) are
+  // returned as `unknown` from the worklet — consumers cast or pass them
+  // through to UI code that knows the shapes.
+  // ==========================================================================
+
+  // Arkade methods — call RN-side arkadeAccount directly (not HRPC worklet)
+
+  async getBoardingAddress(_index: number = 0): Promise<string> {
+    if (!this.arkadeAccount) throw new Error('Arkade wallet not initialized');
+    return await this.arkadeAccount.getBoardingAddress();
+  }
+
+  async createLightningInvoice(
+    amount: number,
+    description?: string,
+    _index: number = 0
+  ): Promise<{ invoice: string; paymentHash: string }> {
+    if (!this.arkadeAccount) throw new Error('Arkade wallet not initialized');
+    return await this.arkadeAccount.createLightningInvoice(amount, description);
+  }
+
+  async getPendingLightningReceives(_index: number = 0): Promise<unknown[]> {
+    if (!this.arkadeAccount) return [];
+    return await this.arkadeAccount.getPendingLightningReceives();
+  }
+
+  async getPendingLightningSends(_index: number = 0): Promise<unknown[]> {
+    if (!this.arkadeAccount) return [];
+    return await this.arkadeAccount.getPendingLightningSends();
+  }
+
+  async getSwapHistory(_index: number = 0): Promise<unknown[]> {
+    if (!this.arkadeAccount) return [];
+    return await this.arkadeAccount.getSwapHistory();
+  }
+
+  async getLightningLimits(_index: number = 0): Promise<unknown> {
+    if (!this.arkadeAccount) return {};
+    return await this.arkadeAccount.getLightningLimits();
+  }
+
+  async getLightningFees(_index: number = 0): Promise<unknown> {
+    if (!this.arkadeAccount) return {};
+    return await this.arkadeAccount.getLightningFees();
+  }
+
+  async waitForLightningPayment(
+    invoice: string,
+    _index: number = 0
+  ): Promise<{ txid: string }> {
+    if (!this.arkadeAccount) throw new Error('Arkade wallet not initialized');
+    return await this.arkadeAccount.waitForLightningPayment(invoice);
+  }
+
+  async getArkadeTransactionHistory(_index: number = 0): Promise<Transaction[]> {
+    if (!this.arkadeAccount) return [];
+    const arkTxs = await this.arkadeAccount.getTransactionHistory();
+    return (arkTxs || []).map((tx: any) => ({
+      blockchain: 'arkade',
+      blockNumber: 0,
+      transactionHash: tx.key?.redeemTxid || tx.key?.boardingTxid || tx.key?.commitmentTxid || '',
+      transferIndex: 0,
+      token: 'btc',
+      amount: String(tx.amount ?? 0),
+      timestamp: Number(tx.createdAt ?? 0),
+      transactionIndex: 0,
+      logIndex: 0,
+      from: '',
+      to: '',
+    }));
+  }
+
   async createWallet(params: {
     walletName: string;
     prf: Buffer | string;
@@ -613,6 +901,8 @@ class WDKService {
       data: wallet,
     });
 
+    await this.initArkadeWallet(seed);
+
     return wallet;
   }
 
@@ -637,6 +927,14 @@ class WDKService {
     const transactionMap: Record<string, Transaction[]> = {};
     const payloadKeys: string[] = []; // Track the keys in order to match with response array
 
+    // Arkade transactions go through the worklet's arkadeGetTransactionHistory
+    // route (one call per enabled BTC asset, not per network type), since the
+    // SDK returns ArkTransaction[] for the whole account regardless of which
+    // sub-NetworkType the consumer is asking about. We populate the map for
+    // ARKADE/LIGHTNING/ARKADE_BOARDING by mirroring the same list under each
+    // key — the receive/send screens then filter as needed.
+    let arkadeTransactions: Transaction[] | null = null;
+
     enabledAssets.forEach((asset) => {
       const networks = AssetBalanceMap[asset];
       if (!networks) return;
@@ -645,6 +943,10 @@ class WDKService {
         const key = `${networkType}_${asset}`;
         transactionMap[key] = [];
 
+        // Skip arkade variants — they'll be filled in below from the
+        // arkadeGetTransactionHistory call.
+        if (isArkadeNetwork(networkType as NetworkType)) return;
+
         const address = networkAddresses[networkType as NetworkType];
 
         if (address) {
@@ -659,6 +961,34 @@ class WDKService {
       });
     });
 
+    // Fetch the Arkade transaction history once if BTC has any arkade variant
+    // enabled. The history is the same regardless of which variant we ask for.
+    const btcNetworks = AssetBalanceMap[AssetTicker.BTC];
+    const hasArkadeVariant = (
+      Object.keys(btcNetworks ?? {}) as NetworkType[]
+    ).some((n) => isArkadeNetwork(n));
+    if (enabledAssets.includes(AssetTicker.BTC) && hasArkadeVariant) {
+      try {
+        arkadeTransactions = await this.getArkadeTransactionHistory();
+      } catch (err) {
+        console.warn('[arkade] Failed to fetch transaction history:', err);
+        arkadeTransactions = [];
+      }
+      // Mirror the same list under each enabled arkade variant key.
+      for (const networkType of Object.keys(
+        btcNetworks ?? {}
+      ) as NetworkType[]) {
+        if (!isArkadeNetwork(networkType)) continue;
+        transactionMap[`${networkType}_${AssetTicker.BTC}`] =
+          arkadeTransactions;
+      }
+    }
+
+    // Short-circuit the WDK indexer call if every enabled entry was arkade.
+    if (payload.length === 0) {
+      return transactionMap;
+    }
+
     const requestOptions = {
       method: 'POST',
       headers: headers,
@@ -699,6 +1029,10 @@ class WDKService {
       }
     >
   > {
+    // Split arkade-keyed entries off the indexer batch path — the WDK
+    // indexer at wdk-api.tether.io doesn't understand the arkade chain, so
+    // those balances come from the RN-side REST workaround in
+    // resolveArkadeBalances. The two sub-results are merged below.
     const headers = new Headers();
     headers.append('accept', 'application/json');
     headers.append('x-api-key', this.getIndexerApiKey());
@@ -715,6 +1049,9 @@ class WDKService {
       if (!networks) return;
 
       Object.keys(networks).forEach((networkType) => {
+        // Skip arkade variants — they go through resolveArkadeBalances.
+        if (isArkadeNetwork(networkType as NetworkType)) return;
+
         const address = networkAddresses[networkType as NetworkType];
         if (address) {
           payload.push({
@@ -726,6 +1063,23 @@ class WDKService {
       });
     });
 
+    const [indexerMap, arkadeMap] = await Promise.all([
+      payload.length > 0 ? this._fetchIndexerBalances(payload, headers) : {},
+      this.resolveArkadeBalances(enabledAssets, networkAddresses),
+    ]);
+
+    return { ...indexerMap, ...arkadeMap };
+  }
+
+  /**
+   * @private
+   * The original WDK indexer batch call, factored out so resolveWalletBalances
+   * can short-circuit it when no non-arkade entries are enabled.
+   */
+  private async _fetchIndexerBalances(
+    payload: { blockchain: string; token: AssetTicker; address: string }[],
+    headers: Headers
+  ): Promise<Record<string, { balance: number; asset: AssetTicker }>> {
     const requestOptions = {
       method: 'POST',
       headers: headers,
@@ -743,14 +1097,22 @@ class WDKService {
 
     const data = await response.json();
 
-    const balanceMap = Object.entries(
+    return Object.entries(
       data as Record<
         string,
         { tokenBalance: { amount: number; blockchain: string; token: string } }
       >
     ).reduce(
       (allBalances, [_, value]) => {
-        const obj = (value as any).tokenBalance;
+        // The WDK indexer returns one entry per payload item, but entries
+        // can come back with `tokenBalance: null` (or be missing entirely)
+        // when the underlying address/blockchain query failed server-side.
+        // Skipping malformed entries here keeps the rest of the wallet
+        // usable instead of crashing the whole balance refresh.
+        const obj = (value as any)?.tokenBalance;
+        if (!obj || !obj.blockchain || !obj.token) {
+          return allBalances;
+        }
         allBalances[`${obj.blockchain}_${obj.token}`] = {
           balance: parseFloat(obj.amount),
           asset: obj.token as AssetTicker,
@@ -759,8 +1121,46 @@ class WDKService {
       },
       {} as Record<string, { balance: number; asset: AssetTicker }>
     );
+  }
+
+  /**
+   * Resolve Arkade balances via the RN-side wallet. Now that the wallet runs
+   * on the RN side with Expo providers, we just call wallet.getBalance()
+   * directly — no more inline bech32m decoder or manual REST workaround.
+   */
+  async resolveArkadeBalances(
+    enabledAssets: AssetTicker[],
+    _networkAddresses: Partial<Record<NetworkType, string>>
+  ): Promise<Record<string, { balance: number; asset: AssetTicker }>> {
+    const result: Record<string, { balance: number; asset: AssetTicker }> = {};
+    if (!this.arkadeAccount) return result;
 
-    return balanceMap;
+    for (const asset of enabledAssets) {
+      const networks = AssetBalanceMap[asset];
+      if (!networks || !(NetworkType.ARKADE in networks)) continue;
+
+      try {
+        const balanceSats = await this.arkadeAccount.getBalance();
+        const btc = Number(balanceSats) / this.getDenominationValue(AssetTicker.BTC);
+        result[`${NetworkType.ARKADE}_${asset}`] = { balance: btc, asset };
+        if (NetworkType.LIGHTNING in networks) {
+          result[`${NetworkType.LIGHTNING}_${asset}`] = { balance: btc, asset };
+        }
+        if (NetworkType.ARKADE_BOARDING in networks) {
+          result[`${NetworkType.ARKADE_BOARDING}_${asset}`] = { balance: btc, asset };
+        }
+      } catch (err) {
+        console.warn('[arkade] Failed to fetch balance:', err);
+        result[`${NetworkType.ARKADE}_${asset}`] = { balance: 0, asset };
+        if (NetworkType.LIGHTNING in networks) {
+          result[`${NetworkType.LIGHTNING}_${asset}`] = { balance: 0, asset };
+        }
+        if (NetworkType.ARKADE_BOARDING in networks) {
+          result[`${NetworkType.ARKADE_BOARDING}_${asset}`] = { balance: 0, asset };
+        }
+      }
+    }
+    return result;
   }
 }
 
diff --git a/src/services/wdk-service/types.ts b/src/services/wdk-service/types.ts
index 6663b0e..8fb7f41 100644
--- a/src/services/wdk-service/types.ts
+++ b/src/services/wdk-service/types.ts
@@ -7,6 +7,8 @@ export enum AssetTicker {
 export enum NetworkType {
   SEGWIT = 'bitcoin',
   LIGHTNING = 'lightning',
+  ARKADE = 'arkade',
+  ARKADE_BOARDING = 'arkade-boarding',
   ETHEREUM = 'ethereum',
   SOLANA = 'solana',
   TRON = 'tron',
@@ -15,9 +17,25 @@ export enum NetworkType {
   ARBITRUM = 'arbitrum',
 }
 
+// Arkade variants — each maps a single BTC asset to a different "mode" on the
+// same underlying Arkade account: ARKADE = offchain Ark address, ARKADE_BOARDING
+// = on-chain BTC boarding deposit, LIGHTNING = swap-backed Lightning invoice.
+// All three resolve to the same worklet chain key ('arkade').
+export const ARKADE_NETWORK_TYPES = [
+  NetworkType.ARKADE,
+  NetworkType.ARKADE_BOARDING,
+  NetworkType.LIGHTNING,
+] as const;
+
+export const isArkadeNetwork = (n: NetworkType): boolean =>
+  (ARKADE_NETWORK_TYPES as readonly NetworkType[]).includes(n);
+
 export const AssetAddressMap = {
   [AssetTicker.BTC]: {
     [NetworkType.SEGWIT]: 'bitcoin',
+    [NetworkType.ARKADE]: 'arkade',
+    [NetworkType.ARKADE_BOARDING]: 'arkade',
+    [NetworkType.LIGHTNING]: 'arkade',
   },
   [AssetTicker.USDT]: {
     [NetworkType.ETHEREUM]: 'ethereum',
@@ -33,6 +51,9 @@ export const AssetAddressMap = {
 export const AssetBalanceMap = {
   [AssetTicker.BTC]: {
     [NetworkType.SEGWIT]: 'bitcoin',
+    [NetworkType.ARKADE]: 'arkade',
+    [NetworkType.ARKADE_BOARDING]: 'arkade',
+    [NetworkType.LIGHTNING]: 'arkade',
   },
   [AssetTicker.USDT]: {
     [NetworkType.ETHEREUM]: 'ethereum',
@@ -113,6 +134,31 @@ export interface BitcoinChainConfig {
   port: number;
 }
 
+/**
+ * Configuration for the Arkade chain (Ark protocol).
+ *
+ * `arkServerUrl` is the only required field — it's the REST endpoint of the
+ * Ark server the wallet talks to. `swapProviderUrl` enables Lightning send/
+ * receive via a Boltz instance; without it, Lightning receive throws and
+ * Lightning send falls back to "no swap provider configured".
+ *
+ * `esploraUrl` is the REST endpoint used by the RN-side balance workaround
+ * to read the on-chain BTC boarding balance. The SDK's bundled Esplora URL
+ * defaults to localhost which is unreachable from a phone — until that's
+ * fixed, the provider hits this URL directly. See README "Temporary Workarounds".
+ */
+export interface ArkadeChainConfig {
+  arkServerUrl: string;
+  /** VTXO indexer / "virtual mempool" URL. If omitted the SDK defaults to the
+   *  arkServerUrl's built-in indexer, which works when the Ark server bundles
+   *  its own indexer. For mutinynet / mainnet setups where the indexer lives at
+   *  a separate domain (e.g. https://explorer.mutinynet.arkade.sh) this must
+   *  be set explicitly. */
+  indexerUrl?: string;
+  swapProviderUrl?: string;
+  esploraUrl?: string;
+}
+
 export interface TronChainConfig {
   chainId: number;
   provider: string;
@@ -133,5 +179,6 @@ export interface ChainsConfig {
   polygon?: EVMChainConfig;
   ton?: TONChainConfig;
   bitcoin?: BitcoinChainConfig;
+  arkade?: ArkadeChainConfig;
   tron?: TronChainConfig;
 }
