diff --git a/src/wdk-worklet.js b/src/wdk-worklet.js
new file mode 100644
index 0000000..4c9d172
--- /dev/null
+++ b/src/wdk-worklet.js
@@ -0,0 +1,21 @@
+/* eslint-disable no-undef */
+const { registerRpcHandlers } = require('../worklet')
+const HRPC = require('../generated/hrpc')
+
+// WDK core + Arkade wallet manager
+const WDK = require('@tetherto/wdk')
+const WalletManagerArkade = require('@arkade-os/wdk')
+
+const { IPC } = BareKit
+const rpc = new HRPC(IPC)
+
+const context = {
+  WDK: WDK.default || WDK,
+  walletManagers: {
+    arkade: WalletManagerArkade.default || WalletManagerArkade
+  },
+  protocolManagers: {},
+  wdk: null
+}
+
+registerRpcHandlers(rpc, context)
